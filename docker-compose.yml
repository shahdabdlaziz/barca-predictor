services:
  barca-api:
    image: shahod/barca-predictor:latest
    container_name: barca-predictor-container
    working_dir: /app
    command: uvicorn api:app --host 0.0.0.0 --port 8000
    ports:
      - "9002:8000"
    restart: always